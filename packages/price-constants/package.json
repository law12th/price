{
	"name": "price-constants",
	"version": "1.0.0",
	"main": "./build/index.js",
	"types": "./build/index.d.ts",
	"scripts": {
		"clean": "del ./build/*",
		"build": "npm run clean && tsc"
	},
	"devDependencies": {
		"tsconfig": "workspace:*",
		"eslint-config-custom": "workspace:*"
	}
}
